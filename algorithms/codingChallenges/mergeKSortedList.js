const mergeTwoList = (l1, l2) => {
 let list = new List()
 
}

mergeSortedList = (list) => {
 if(list.length === 0) return null;
 while(list.length > 1) {
  let l1 = list.shif()
  let l2 = list.shif()
  let combined = mergeTwoList(l1, l2)
 }
}

